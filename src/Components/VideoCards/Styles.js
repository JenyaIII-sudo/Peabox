import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  cardsGrid: {
    height: "18vh"
  },
  gridContainer: {
    paddingTop: "40px"
  }
}));

export default useStyles;
