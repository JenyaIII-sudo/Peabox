import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  filterBarContent: {
    display: "flex",
    alignItems: "center"
  },
  logoMini: {
    paddingLeft: "20px"
  },
  form: {
    width: "100%"
  }
}));

export default useStyles;
